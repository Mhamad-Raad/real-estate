# --- Local AI guidance / config (kept out of the repo per project preference) ---
CLAUDE.md
.claude/settings.json
.claude/settings.local.json

# --- Secrets / env ---
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key

# --- Runtime data (lives in Desktop/LandAllocationData, never in repo) ---
/data/
*.dump
*.sql

# --- Python / Django / Celery ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
*.log
db.sqlite3
staticfiles/
mediafiles/
celerybeat-schedule*
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# --- Node / React / Vite ---
node_modules/
frontend/dist/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Docker ---
# (compose files ARE tracked; only local overrides are ignored)
docker-compose.override.yml

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# OCR samples — real government ID scans. Personal data: never commit these.
*.jpeg
*.jpg
*.png
!frontend/public/**
samples/

# Acceptance-testing findings (It.7) — local working notes, not repo content.
# May quote real pilot records, so it deliberately stays out of git.
/FINDINGS.md
